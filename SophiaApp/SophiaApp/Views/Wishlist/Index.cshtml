@{
    ViewData["Title"] = "Blog";
}
<main>
    <section id="breadcrumb-area">
        <div class="container">
            <a asp-controller="Wishlist" asp-action="Index">Wishlist</a>
            <span>
                <a asp-controller="Home" asp-action="Index">Home / </a>
                <a asp-controller="Wishlist" asp-action="Index">Wishlist</a>
            </span>
        </div>
        <hr>
    </section>
    <section id="main">
        <div class="container">
            <div class="scroll-table-area">
                <div class="table-area d-none">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">Product</th>
                                <th scope="col">Name </th>
                                <th scope="col">Price</th>
                                <th scope="col"><button>Clear All</button></th>
                            </tr>
                        </thead>
                        <tbody>
                        </tbody>
                    </table>
                    <div class="footer d-flex align-items-center justify-content-between">
                        <a asp-controller="Home" asp-action="Index"><button>Continue Shopping</button> </a>
                        <div class="footer-right">
                            <span class="total-price"> </span>
                            <a asp-controller="Checkout" asp-action="Index"><button class="checkout">Proceed To Checkout</button> </a>
                        </div>
                    </div>
                </div>
            </div>

            <div class="msg">
                <div class="empty-msg">
                    <p>YOU HAVE NO ITEMS IN YOUR WISHLIST.</p>
                </div>
                <a asp-controller="Home" asp-action="Index"><button>Continue Shopping</button></a>
            </div>
        </div>
    </section>
</main>
@section Scripts {
    <script src="../assets/js/wishlist.js"></script>
}